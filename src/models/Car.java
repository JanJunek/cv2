package models;

public class Car extends Vehicle {
    private String model;

    //kdyz se dedi musi byt constructor
    public Car(String brand) {
        super(brand);
        this.model = model;
    }
}
